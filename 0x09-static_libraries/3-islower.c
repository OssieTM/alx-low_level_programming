#include "main.h"
/**
 * _islower - checking for small letters
 * @c: the character to be checked
 * Return: 1 if c is lowercase, or 0 if not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
